import chai from 'github-webscrap/config/tests/chai'
import { repositoryProcessor } from 'github-webscrap/modules'

describe('Repository processing', ()=> {
  describe('Processing total number of bytes', done => {
    // repositoryProcessor({ host: 'raw.githubusercontent.com' })  
  })
})